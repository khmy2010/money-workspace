export enum QueryRangeMode {
  TODAY = 'Today',
  YESTERDAY = 'Yesterday',
  CURRENT_WEEK = 'Current Week',
  CURRENT_MONTH = 'Current Month',
}

export enum QueryMode {
  RANGE = 'Range',
  AMOUNT = 'Amount',
  CATEGORY = 'Category',
}